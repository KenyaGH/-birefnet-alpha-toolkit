name: Release - Alpha Mask Toolkit

on:
  push:
    tags:
      - 'v*.*.*'  # Semantic versioning tags (v1.0.0, v2.1.3, etc.)

env:
  REGISTRY: docker.io
  IMAGE_NAME: alpha-mask-toolkit

jobs:
  # Build and release
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for changelog

    - name: 🔍 Validate tag format
      run: |
        TAG=${GITHUB_REF#refs/tags/}
        if [[ ! $TAG =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          echo "❌ Invalid tag format: $TAG"
          echo "Expected format: v1.0.0"
          exit 1
        fi
        echo "✅ Valid tag format: $TAG"
        echo "TAG=$TAG" >> $GITHUB_ENV

    - name: 🔐 Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: 🏷️ Tag and push release versions
      run: |
        # Extract version numbers from tag
        VERSION=${TAG#v}  # Remove 'v' prefix
        MAJOR=$(echo $VERSION | cut -d. -f1)
        MINOR=$(echo $VERSION | cut -d. -f1-2)

        IMAGE_BASE="${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"

        echo "Creating release tags for version: $VERSION"

        # Pull the latest image built by CD pipeline
        docker pull $IMAGE_BASE:latest

        # Tag with semantic versions
        docker tag $IMAGE_BASE:latest $IMAGE_BASE:$TAG
        docker tag $IMAGE_BASE:latest $IMAGE_BASE:$VERSION
        docker tag $IMAGE_BASE:latest $IMAGE_BASE:$MINOR
        docker tag $IMAGE_BASE:latest $IMAGE_BASE:$MAJOR

        # Push all tags
        docker push $IMAGE_BASE:$TAG
        docker push $IMAGE_BASE:$VERSION
        docker push $IMAGE_BASE:$MINOR
        docker push $IMAGE_BASE:$MAJOR

        echo "✅ Release tags created:"
        echo "  - $IMAGE_BASE:$TAG"
        echo "  - $IMAGE_BASE:$VERSION"
        echo "  - $IMAGE_BASE:$MINOR"
        echo "  - $IMAGE_BASE:$MAJOR"

        # Save tags for summary
        echo "RELEASE_TAGS<<EOF" >> $GITHUB_ENV
        echo "$IMAGE_BASE:$TAG" >> $GITHUB_ENV
        echo "$IMAGE_BASE:$VERSION" >> $GITHUB_ENV
        echo "$IMAGE_BASE:$MINOR" >> $GITHUB_ENV
        echo "$IMAGE_BASE:$MAJOR" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV

    - name: 📝 Generate release notes
      id: release_notes
      run: |
        # Get previous tag
        PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

        # Generate changelog
        if [ -z "$PREV_TAG" ]; then
          COMMITS=$(git log --oneline --pretty=format:"- %s (%h)" | head -20)
          COMPARE_URL=""
        else
          COMMITS=$(git log ${PREV_TAG}..HEAD --oneline --pretty=format:"- %s (%h)")
          COMPARE_URL="**Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREV_TAG}...${{ env.TAG }}"
        fi

        # Check for breaking changes
        BREAKING_CHANGES=""
        if git log ${PREV_TAG}..HEAD --grep="BREAKING CHANGE" --oneline | grep -q .; then
          BREAKING_CHANGES="## ⚠️ BREAKING CHANGES
        $(git log ${PREV_TAG}..HEAD --grep="BREAKING CHANGE" --pretty=format:"- %s")

        "
        fi

        # Create release notes
        RELEASE_NOTES="${BREAKING_CHANGES}## 🚀 What's New in ${{ env.TAG }}

        ### 🔄 Changes:
        $COMMITS

        ### 🐳 Docker Images:
        \`\`\`bash
        # Pull the latest release
        docker pull ${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}

        # Run alpha mask generation
        docker run --gpus all \\
          -v ./input:/app/input \\
          ${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}
        \`\`\`

        ### 📦 Available Tags:
        - \`${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}\` - This specific version
        - \`${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest\` - Latest stable release

        ### 🔧 Requirements:
        - **GPU**: NVIDIA GPU with CUDA support (recommended for best performance)
        - **Runtime**: Docker with NVIDIA Container Runtime
        - **Input**: Images in folders ending with \`_converted\`

        ### 📚 Documentation:
        - [Docker Usage Guide](README_DOCKER.md)
        - [Alpha Mask Creation Guide](README_ALPHA_MASKS.md)

        $COMPARE_URL"

        # Save to environment
        echo "RELEASE_NOTES<<EOF" >> $GITHUB_ENV
        echo "$RELEASE_NOTES" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV

    - name: 🏷️ Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.TAG }}
        release_name: Alpha Mask Toolkit ${{ env.TAG }}
        body: ${{ env.RELEASE_NOTES }}
        draft: false
        prerelease: ${{ contains(env.TAG, '-') }}

    - name: 📊 Post-release summary
      run: |
        echo "## 🎉 Release ${{ env.TAG }} Published!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Docker Images:" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "${{ env.RELEASE_TAGS }}" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🔗 Links:" >> $GITHUB_STEP_SUMMARY
        echo "- [GitHub Release](https://github.com/${{ github.repository }}/releases/tag/${{ env.TAG }})" >> $GITHUB_STEP_SUMMARY
        echo "- [Docker Hub](https://hub.docker.com/r/${{ github.repository_owner }}/${{ env.IMAGE_NAME }})" >> $GITHUB_STEP_SUMMARY